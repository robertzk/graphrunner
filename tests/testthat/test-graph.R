describe("graphNode", {
  context("graphNode")
  
  test_that("it can initialize correctly", {
    expect_true(TRUE)
  })
})

describe("graph", {
  context("graph")
  
  test_that("it can initialize correctly", {
    expect_true(TRUE)
  })
})

